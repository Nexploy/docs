---
title: Domini
description: Gestire i domini e le route dei tuoi deployment
---

Gestisci i domini associati ai tuoi deployment, aggiungi o rimuovi route e associa certificati SSL, direttamente dall'interfaccia o tramite i nodi di pipeline (`add-domain`, `remove-domain`, `add-ssl-certificate`).
